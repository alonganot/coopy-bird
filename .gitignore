node_modules/
dist/
dist-ssr/
dev-dist/
*.local
.env
.env.local
# Stale local artifact from the pre-Postgres JSON-file leaderboard; unused now.
server/data/

# Local Claude Code settings (personal, not shared)
.claude/settings.local.json
